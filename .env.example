# Media Server Configuration
# Only configure what you need - everything else is auto-detected

# ============ STORAGE ============
# Where to store media files
# Examples:
#   ./data              (relative to this directory)
#   /data               (absolute path)
#   /mnt/external/media (external drive)
DATA_ROOT=./data

# ============ MULLVAD VPN ============
# Get these from: https://mullvad.net/en/account/wireguard-config
# Download the .conf file and copy values from [Interface] section:

WIREGUARD_PRIVATE_KEY=your-private-key-here=
WIREGUARD_ADDRESSES=10.x.x.x/32

# ============ ADVANCED (Optional) ============
# VPN server location (leave empty for auto)
VPN_SERVER_CITIES=

# Config storage (leave as default)
CONFIG_ROOT=./config
