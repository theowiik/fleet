# ═══════════════════════════════════════════════════════════════════════
# MEDIA SERVER STACK - ENVIRONMENT CONFIGURATION
# Copy this file to .env and fill in your values
# ═══════════════════════════════════════════════════════════════════════

# ============ SYSTEM CONFIGURATION ============
# Run these commands to find your values:
#   id -u        (PUID)
#   id -g        (PGID)
#   getent group render | cut -d: -f3  (RENDER_GID)

PUID=1000
PGID=1000
RENDER_GID=122
TZ=Europe/Stockholm

# ============ NETWORK CONFIGURATION ============
# Your local network subnet (for VPN firewall rules)
# Examples: 192.168.1.0/24, 192.168.0.0/24, 10.0.0.0/24
LAN_SUBNET=192.168.1.0/24

# ============ STORAGE PATHS ============
# Main data directory (your external disk mount point)
DATA_ROOT=/data

# Docker app configs (databases, settings)
CONFIG_ROOT=/docker/appdata

# ============ MULLVAD WIREGUARD VPN ============
# Get these from: https://mullvad.net/en/account/wireguard-config
# 
# 1. Login to Mullvad account
# 2. Go to WireGuard configuration
# 3. Generate a key or use existing
# 4. Copy the values below

VPN_PROVIDER=mullvad
VPN_TYPE=wireguard

# From [Interface] section of Mullvad config:
WIREGUARD_PRIVATE_KEY=your-private-key-here
WIREGUARD_ADDRESSES=10.x.x.x/32

# Server location (examples: Stockholm, Amsterdam, London)
# Leave empty for random server
VPN_SERVER_CITIES=Stockholm

# ============ JELLYFIN ============
JELLYFIN_PORT=8096
JELLYFIN_DISCOVERY_PORT=7359

# ============ JELLYSEERR ============
JELLYSEERR_PORT=5055

# ============ RADARR (Movies) ============
RADARR_PORT=7878

# ============ SONARR (TV Shows) ============
SONARR_PORT=8989

# ============ PROWLARR (Indexer Manager) ============
PROWLARR_PORT=9696

# ============ BAZARR (Subtitles) ============
BAZARR_PORT=6767

# ============ QBITTORRENT (via VPN) ============
QBITTORRENT_PORT=8080

# ============ GLUETUN VPN ============
GLUETUN_CONTROL_PORT=8000
